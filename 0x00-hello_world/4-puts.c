#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char Mystr[] = "Programming is like building a multilingual puzzle\n";

	putst(Mystr);

	return (0);
}
